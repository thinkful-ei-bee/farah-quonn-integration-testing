const expect = require('chai').expect;
const sort = require('../app'); 
const request = require('supertest');
const app = require('../app');

describe('Sort function', () => {
    it('should contain all numbers', () => {
        const j = 10;
        if(Number.isNaN(j)) {
            return "J should be a number";
        };
        
    })
});